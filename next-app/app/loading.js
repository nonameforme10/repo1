export default function Loading() {
  return (
    <main className="launcher-empty-state">
      <section className="state-panel" aria-live="polite">
        <h1>Loading EduVenture</h1>
        <p>Preparing the Next.js launch shell so the heavier client workspace can open cleanly.</p>
      </section>
    </main>
  );
}
